#pragma once

#include <cstdint>

namespace BooleanEngine
{

constexpr int MIN_SUPPORTED_VARIABLES = 2;

constexpr int MAX_SUPPORTED_VARIABLES = 4;

constexpr uint32_t INVALID_VERTEX = UINT32_MAX;

}
